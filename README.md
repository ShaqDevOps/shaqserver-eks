# shaqserver-eks

Terraform configuration for the ShaqServer EKS infrastructure.

## Safety

Do not commit Terraform state, local provider caches, kubeconfigs, credentials, or generated command output. Those files are intentionally ignored in `.gitignore`.
